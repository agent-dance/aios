import React from 'react';
import ReactDOM from 'react-dom/client';
import { SpaceGameApp } from './SpaceGameApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SpaceGameApp />
  </React.StrictMode>,
);
